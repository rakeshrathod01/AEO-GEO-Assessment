{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "backend"
  },
  "deploy": {
    "startCommand": "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/api/v1/health"
  },
  "_notes": [
    "Railway: New Project -> Deploy from repo. Set Root Directory to 'backend'.",
    "Add the Redis and Postgres plugins; Railway injects REDIS_URL and DATABASE_URL.",
    "Set SECRET_KEY and AUTH_REQUIRED=true in Variables.",
    "Add a second service with start command:",
    "  celery -A app.core.celery_app.celery_app worker --loglevel=info"
  ]
}
